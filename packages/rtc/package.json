{
  "name": "@nearstack-dev/rtc",
  "version": "0.2.0",
  "description": "WebRTC + CRDT sync layer (stub for now)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "node ../../scripts/clean-dist.mjs dist && tsc",
    "dev": "tsc --watch",
    "test": "echo \"No tests yet\"",
    "prepublishOnly": "pnpm build"
  },
  "author": "Dakota Kim",
  "license": "MIT",
  "dependencies": {
    "@nearstack-dev/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
